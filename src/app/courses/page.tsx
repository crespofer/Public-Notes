export default function Courses() {
  // Will show all courses avaible as cards
  // You can click on each course to navigate to courses/:id (need authentiction)

  return (
    <div>
      <h1 className="text-red-700 pt-14">Courses</h1>
    </div>
  );
}
